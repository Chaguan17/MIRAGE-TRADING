server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html;
    index index.html;

    # Fallback necesario para React Router (rutas client-side)
    location / {
        try_files $uri $uri/ /index.html;
    }
}
